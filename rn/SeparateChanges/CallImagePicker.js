import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import Button from '../components/Button.jsx';

const CallImagePicker = () => {
  return (
    <View>
      <Button title={'Image Picker'} />
    </View>
  );
};

export default CallImagePicker;

